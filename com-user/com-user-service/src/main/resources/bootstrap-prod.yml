spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        metadata:
          management:
            context-path: ${server.servlet.context-path}/actuator
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        #默认为Public命名空间,可以省略不写  自定义命名空间使用ID值
        namespace: e478917f-b947-4a94-9358-eed95f37a89f